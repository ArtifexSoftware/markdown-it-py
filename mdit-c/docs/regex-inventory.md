# Regex inventory — `markdown_it`

_Generated by `mdit-c/scripts/regex_inventory.py`. Re-run after any source change._

- **Total call sites:** 55
- **Dynamic patterns (literal not recoverable):** 4
- **Source root:** `markdown_it`

## By function

| Function | Count |
| --- | ---: |
| `re.compile` | 54 |
| `re.sub` | 1 |

## By feature (cumulative across all patterns)

| Feature | Count |
| --- | ---: |
| `anchors` | 31 |
| `char_classes` | 22 |
| `alternation` | 19 |
| `case_insensitive` | 11 |
| `predefined_classes` | 8 |
| `lookarounds` | 4 |
| `multiline` | 3 |
| `nongreedy` | 1 |

## By file

| File | Count |
| --- | ---: |
| `markdown_it/rules_block/html_block.py` | 14 |
| `markdown_it/rules_core/replacements.py` | 10 |
| `markdown_it/common/utils.py` | 8 |
| `markdown_it/rules_core/linkify.py` | 3 |
| `markdown_it/_punycode.py` | 2 |
| `markdown_it/common/html_re.py` | 2 |
| `markdown_it/common/normalize_url.py` | 2 |
| `markdown_it/parser_inline.py` | 2 |
| `markdown_it/rules_block/table.py` | 2 |
| `markdown_it/rules_core/normalize.py` | 2 |
| `markdown_it/rules_core/smartquotes.py` | 2 |
| `markdown_it/rules_inline/autolink.py` | 2 |
| `markdown_it/rules_inline/entity.py` | 2 |
| `markdown_it/rules_inline/backticks.py` | 1 |
| `markdown_it/rules_inline/linkify.py` | 1 |

## All call sites

### `markdown_it/_punycode.py`

#### markdown_it/_punycode.py:27 — `re.compile` in `<module>`

- **Flags:** —
- **Features:** `char_classes`

```
[\x2E\u3002\uFF0E\uFF61]
```

#### markdown_it/_punycode.py:28 — `re.compile` in `<module>`

- **Flags:** —
- **Features:** `anchors`, `char_classes`

```
[^\0-\x7E]
```

### `markdown_it/common/html_re.py`

#### markdown_it/common/html_re.py:23 — `re.compile` in `<module>`

- **Flags:** —
- **Features:** `anchors`, `char_classes`, `predefined_classes`, `nongreedy`, `alternation`

```
^(?:<[A-Za-z][A-Za-z0-9\-]*(?:\s+[a-zA-Z_:][a-zA-Z0-9:._-]*(?:\s*=\s*(?:[^"'=<>`\x00-\x20]+|'[^']*'|"[^"]*"))?)*\s*\/?>|<\/[A-Za-z][A-Za-z0-9\-]*\s*>|<!---?>|<!--(?:[^-]|-[^-]|--[^>])*-->|<[?][\s\S]*?[?]>|<![A-Za-z][^>]*>|<!\[CDATA\[[\s\S]*?\]\]>)
```

#### markdown_it/common/html_re.py:39 — `re.compile` in `<module>`

- **Flags:** —
- **Features:** `anchors`, `char_classes`, `predefined_classes`, `alternation`

```
^(?:<[A-Za-z][A-Za-z0-9\-]*(?:\s+[a-zA-Z_:][a-zA-Z0-9:._-]*(?:\s*=\s*(?:[^"'=<>`\x00-\x20]+|'[^']*'|"[^"]*"))?)*\s*\/?>|<\/[A-Za-z][A-Za-z0-9\-]*\s*>)
```

### `markdown_it/common/normalize_url.py`

#### markdown_it/common/normalize_url.py:66 — `re.compile` in `<module>`

- **Flags:** —
- **Features:** `anchors`, `alternation`

```
^(vbscript|javascript|file|data):
```

#### markdown_it/common/normalize_url.py:67 — `re.compile` in `<module>`

- **Flags:** —
- **Features:** `anchors`, `alternation`

```
^data:image\/(gif|png|jpeg|webp);
```

### `markdown_it/common/utils.py`

#### markdown_it/common/utils.py:90 — `re.compile` in `<module>`

- **Flags:** `re.IGNORECASE`
- **Features:** `anchors`, `char_classes`, `alternation`, `case_insensitive`

```
\\([!"#$%&\'()*+,\-.\/:;<=>?@[\\\]^_`{|}~])|&([a-z#][a-z0-9]{1,31});
```

#### markdown_it/common/utils.py:94 — `re.compile` in `<module>`

- **Flags:** —
- **Features:** `char_classes`

```
#([0-9]{1,8})
```

#### markdown_it/common/utils.py:95 — `re.compile` in `<module>`

- **Flags:** `re.IGNORECASE`
- **Features:** `char_classes`, `case_insensitive`

```
#x([a-f0-9]{1,8})
```

#### markdown_it/common/utils.py:131 — `re.compile` in `<module>`

- **Flags:** —
- **Features:** `anchors`, `char_classes`, `alternation`

```
\\([\\!"#$%&'()*+,./:;<=>?@\[\]^`{}|_~-])
```

#### markdown_it/common/utils.py:151 — `re.compile` in `<module>`

- **Flags:** —
- **Features:** `anchors`, `char_classes`, `alternation`

```
[.?*+^$[\]\\(){}|-]
```

#### markdown_it/common/utils.py:257 — `re.sub` in `normalizeReference`

- **Flags:** `" "`
- **Features:** `predefined_classes`

```
\s+
```

#### markdown_it/common/utils.py:304 — `re.compile` in `<module>`

- **Flags:** `re.IGNORECASE`
- **Features:** `anchors`, `char_classes`, `predefined_classes`, `case_insensitive`

```
^<a[>\s]
```

#### markdown_it/common/utils.py:305 — `re.compile` in `<module>`

- **Flags:** `re.IGNORECASE`
- **Features:** `anchors`, `predefined_classes`, `case_insensitive`

```
^</a\s*>
```

### `markdown_it/parser_inline.py`

#### markdown_it/parser_inline.py:58 — `re.compile` in `_default_terminator_re`

- **Flags:** —
- **Features:** _unknown_

_(dynamic)_

#### markdown_it/parser_inline.py:124 — `re.compile` in `ParserInline.add_terminator_char`

- **Flags:** —
- **Features:** _unknown_

_(dynamic)_

### `markdown_it/rules_block/html_block.py`

#### markdown_it/rules_block/html_block.py:17 — `re.compile` in `<module>`

- **Flags:** `re.IGNORECASE`
- **Features:** `anchors`, `predefined_classes`, `lookarounds`, `alternation`, `case_insensitive`

```
^<(script|pre|style|textarea)(?=(\s|>|$))
```

#### markdown_it/rules_block/html_block.py:18 — `re.compile` in `<module>`

- **Flags:** `re.IGNORECASE`
- **Features:** `alternation`, `case_insensitive`

```
<\/(script|pre|style|textarea)>
```

#### markdown_it/rules_block/html_block.py:21 — `re.compile` in `<module>`

- **Flags:** —
- **Features:** `anchors`

```
^<!--
```

#### markdown_it/rules_block/html_block.py:21 — `re.compile` in `<module>`

- **Flags:** —
- **Features:** 

```
-->
```

#### markdown_it/rules_block/html_block.py:22 — `re.compile` in `<module>`

- **Flags:** —
- **Features:** `anchors`

```
^<\?
```

#### markdown_it/rules_block/html_block.py:22 — `re.compile` in `<module>`

- **Flags:** —
- **Features:** 

```
\?>
```

#### markdown_it/rules_block/html_block.py:23 — `re.compile` in `<module>`

- **Flags:** —
- **Features:** `anchors`, `char_classes`

```
^<![A-Z]
```

#### markdown_it/rules_block/html_block.py:23 — `re.compile` in `<module>`

- **Flags:** —
- **Features:** 

```
>
```

#### markdown_it/rules_block/html_block.py:24 — `re.compile` in `<module>`

- **Flags:** —
- **Features:** `anchors`

```
^<!\[CDATA\[
```

#### markdown_it/rules_block/html_block.py:24 — `re.compile` in `<module>`

- **Flags:** —
- **Features:** 

```
\]\]>
```

#### markdown_it/rules_block/html_block.py:26 — `re.compile` in `<module>`

- **Flags:** `re.IGNORECASE`
- **Features:** _unknown_

_(dynamic)_

#### markdown_it/rules_block/html_block.py:27 — `re.compile` in `<module>`

- **Flags:** —
- **Features:** `anchors`

```
^$
```

#### markdown_it/rules_block/html_block.py:30 — `re.compile` in `<module>`

- **Flags:** —
- **Features:** _unknown_

_(dynamic)_

#### markdown_it/rules_block/html_block.py:30 — `re.compile` in `<module>`

- **Flags:** —
- **Features:** `anchors`

```
^$
```

### `markdown_it/rules_block/table.py`

#### markdown_it/rules_block/table.py:9 — `re.compile` in `<module>`

- **Flags:** —
- **Features:** `anchors`

```
^:?-+:?$
```

#### markdown_it/rules_block/table.py:10 — `re.compile` in `<module>`

- **Flags:** —
- **Features:** `anchors`, `alternation`

```
^\||\|$
```

### `markdown_it/rules_core/linkify.py`

#### markdown_it/rules_core/linkify.py:10 — `re.compile` in `<module>`

- **Flags:** —
- **Features:** `anchors`

```
^http://
```

#### markdown_it/rules_core/linkify.py:11 — `re.compile` in `<module>`

- **Flags:** —
- **Features:** `anchors`

```
^mailto:
```

#### markdown_it/rules_core/linkify.py:12 — `re.compile` in `<module>`

- **Flags:** `re.IGNORECASE`
- **Features:** `anchors`, `case_insensitive`

```
^mailto:
```

### `markdown_it/rules_core/normalize.py`

#### markdown_it/rules_core/normalize.py:8 — `re.compile` in `<module>`

- **Flags:** —
- **Features:** `alternation`

```
\r\n?|\n
```

#### markdown_it/rules_core/normalize.py:9 — `re.compile` in `<module>`

- **Flags:** —
- **Features:** 

```
\0
```

### `markdown_it/rules_core/replacements.py`

#### markdown_it/rules_core/replacements.py:31 — `re.compile` in `<module>`

- **Flags:** —
- **Features:** `alternation`

```
\+-|\.\.|\?\?\?\?|!!!!|,,|--
```

#### markdown_it/rules_core/replacements.py:37 — `re.compile` in `<module>`

- **Flags:** `re.IGNORECASE`
- **Features:** `alternation`, `case_insensitive`

```
\((c|tm|r)\)
```

#### markdown_it/rules_core/replacements.py:39 — `re.compile` in `<module>`

- **Flags:** —
- **Features:** 

```
\+-
```

#### markdown_it/rules_core/replacements.py:41 — `re.compile` in `<module>`

- **Flags:** —
- **Features:** 

```
\.{2,}
```

#### markdown_it/rules_core/replacements.py:43 — `re.compile` in `<module>`

- **Flags:** —
- **Features:** `char_classes`

```
([?!])…
```

#### markdown_it/rules_core/replacements.py:45 — `re.compile` in `<module>`

- **Flags:** —
- **Features:** `char_classes`

```
([?!]){4,}
```

#### markdown_it/rules_core/replacements.py:47 — `re.compile` in `<module>`

- **Flags:** —
- **Features:** 

```
,{2,}
```

#### markdown_it/rules_core/replacements.py:49 — `re.compile` in `<module>`

- **Flags:** `re.MULTILINE`
- **Features:** `anchors`, `char_classes`, `lookarounds`, `alternation`, `multiline`

```
(^|[^-])---(?=[^-]|$)
```

#### markdown_it/rules_core/replacements.py:51 — `re.compile` in `<module>`

- **Flags:** `re.MULTILINE`
- **Features:** `anchors`, `predefined_classes`, `lookarounds`, `alternation`, `multiline`

```
(^|\s)--(?=\s|$)
```

#### markdown_it/rules_core/replacements.py:53 — `re.compile` in `<module>`

- **Flags:** `re.MULTILINE`
- **Features:** `anchors`, `char_classes`, `predefined_classes`, `lookarounds`, `alternation`, `multiline`

```
(^|[^-\s])--(?=[^-\s]|$)
```

### `markdown_it/rules_core/smartquotes.py`

#### markdown_it/rules_core/smartquotes.py:12 — `re.compile` in `<module>`

- **Flags:** —
- **Features:** `char_classes`

```
['\"]
```

#### markdown_it/rules_core/smartquotes.py:13 — `re.compile` in `<module>`

- **Flags:** —
- **Features:** `char_classes`

```
['\"]
```

### `markdown_it/rules_inline/autolink.py`

#### markdown_it/rules_inline/autolink.py:6 — `re.compile` in `<module>`

- **Flags:** —
- **Features:** `anchors`, `char_classes`, `alternation`

```
^([a-zA-Z0-9.!#$%&\'*+\/=?^_`{|}~-]+@[a-zA-Z0-9](?:[a-zA-Z0-9-]{0,61}[a-zA-Z0-9])?(?:\.[a-zA-Z0-9](?:[a-zA-Z0-9-]{0,61}[a-zA-Z0-9])?)*)$
```

#### markdown_it/rules_inline/autolink.py:9 — `re.compile` in `<module>`

- **Flags:** —
- **Features:** `anchors`, `char_classes`

```
^([a-zA-Z][a-zA-Z0-9+.\-]{1,31}):([^<>\x00-\x20]*)$
```

### `markdown_it/rules_inline/backticks.py`

#### markdown_it/rules_inline/backticks.py:6 — `re.compile` in `<module>`

- **Flags:** —
- **Features:** `anchors`

```
^ (.+) $
```

### `markdown_it/rules_inline/entity.py`

#### markdown_it/rules_inline/entity.py:8 — `re.compile` in `<module>`

- **Flags:** `re.IGNORECASE`
- **Features:** `anchors`, `char_classes`, `alternation`, `case_insensitive`

```
^&#((?:x[a-f0-9]{1,6}|[0-9]{1,7}));
```

#### markdown_it/rules_inline/entity.py:9 — `re.compile` in `<module>`

- **Flags:** `re.IGNORECASE`
- **Features:** `anchors`, `char_classes`, `case_insensitive`

```
^&([a-z][a-z0-9]{1,31});
```

### `markdown_it/rules_inline/linkify.py`

#### markdown_it/rules_inline/linkify.py:8 — `re.compile` in `<module>`

- **Flags:** `re.IGNORECASE`
- **Features:** `anchors`, `char_classes`, `alternation`, `case_insensitive`

```
(?:^|[^a-z0-9.+-])([a-z][a-z0-9.+-]*)$
```
